services:
  - type: web
    name: portfolio-backend
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: RESEND_API_KEY
        sync: false
      - key: MAIL_TO
        sync: false
